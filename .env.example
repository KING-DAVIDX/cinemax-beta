SPARK_DATABASE_URL=
SPARK_API_KEY=
AUTH_SECRET=

# Optional MongoDB fallback (used automatically if SparkDB is unavailable).
# Same database/collections as SparkDB. Defaults to SPARK_DATABASE_URL if unset.
MONGODB_URI=

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=

SMTP_USER=cinemax.nexustech@gmail.com
SMTP_PASS=your_gmail_app_password
